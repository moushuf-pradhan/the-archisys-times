import { classes } from './NavBar.styles';

export default function NavBar() {
	return (
		<nav className={classes.wrapper}>
			<div className="border-y-1 border-gray-400 py-3 flex justify-center">
				NavBar
			</div>
		</nav>
	);
}
